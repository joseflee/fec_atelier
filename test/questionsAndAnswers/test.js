const expect = require("chai").expect;

beforeEach(() => {
});

afterEach(() => {
});

describe("Rendering Component", () => {
  it("Renders Component in App", () => {
    expect(1).to.equal(1)
  });
});