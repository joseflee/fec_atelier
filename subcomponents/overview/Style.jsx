import React from 'react';

var Style = (props) => {

  //props (single style name, img and checkmark / selected data are received and style circle is rendered )

  return (
    <div className={'styleSelector'}>single style</div>
  )

}

export default Style;